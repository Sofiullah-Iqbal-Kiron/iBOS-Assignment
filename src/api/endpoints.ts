const API_BASE = 'https://dummyjson.com'

export const endpoints = {
    products: `${API_BASE}/products`,
    categories: `${API_BASE}/products/categories`,
    category_list: `${API_BASE}/products/category-list`,
}
